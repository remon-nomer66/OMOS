コードを変更する箇所は各方法によって異なります
想定例を2つ示します

1．スレッドメイン関数を変更する場合     
    (実際のコードと遠い，変更箇所が少ない)
2．作成した個別のファイルを用いる場合
    (実際のコードに近い，変更箇所が複数ファイルに跨る，
    "TEST"と毎回入力する必要がある)

1．の場合で変更する箇所

    test_service.c
        20行目から48行目を自身のコードで置き換える
    
    Makefile
        "test_func.c"を削除

2．の場合で変更する箇所

    test.h
        90行目: 作成した関数に直す

    test_service.c
        27行目: 関数を変更

    test_func.c
        このファイルは削除し，作成したファイルに置き換える

    Makefile
        6行目: "test_func.c"を作成したファイル名に置き換える


使い方

    "make"でコンパイルし，"./test_server"でサーバを起動

    "gcc test_mclient.c -o tmclient"でコンパイルし，
    "./tmclient 133.54.224.(任意のアドレス) 10000"でサーバに接続


注意

    いつも使っている"mclient"は利用しないで下さい


注釈

    コマンドはtest.hで宣言されていますが，追加・変更する場合はtest.hを変更して下さい

その他

    自身のDBで行いたい場合は，"test_server_main.c"の47行目から49行目を自身のものに
    置き換えて下さい

    SQLは"sqlフォルダ"の中にありますので，テーブルを作成する場合は，
    ディレクトリを"sql"に移動し，DBを操作して下さい

    "\i merged.sql"でテーブルを作成できます