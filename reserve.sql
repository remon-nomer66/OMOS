--
-- reserve テーブル
--
CREATE Table reserve (
       id    	   integer,      --口座ID
       name	   varchar(50),  --口座名
       balance	   integer,      --預金残高
       max_balance integer,      --貯金限度額
       min_balance integer,	 --借入限度額
       PRIMARY KEY (id)
);

CREATE Table reserve (
       id    	   integer,      --口座ID
       name	   varchar(50),  --口座名
       balance	   integer,      --預金残高
       max_balance integer,      --貯金限度額
       min_balance integer,	 --借入限度額
       PRIMARY KEY (id)
);

CREATE Table reserve (
       id    	   integer,      --口座ID
       name	   varchar(50),  --口座名
       balance	   integer,      --預金残高
       max_balance integer,      --貯金限度額
       min_balance integer,	 --借入限度額
       PRIMARY KEY (id)
);